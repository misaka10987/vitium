diff --git a/dist/assets/utils/vendor/image-size/types/svg.js b/dist/assets/utils/vendor/image-size/types/svg.js
index 0f2288168f9ac773f652270a67b12b3347060edb..0b7a43bf203b35c22bf2b5aea7162af16f592062 100644
--- a/dist/assets/utils/vendor/image-size/types/svg.js
+++ b/dist/assets/utils/vendor/image-size/types/svg.js
@@ -71,8 +71,13 @@ function calculateByViewbox(attrs, viewbox) {
   };
 }
 const SVG = {
-  // Scan only the first kilo-byte to speed up the check on larger files
-  validate: (input) => svgReg.test(toUTF8String(input, 0, 1e3)),
+  validate: (input) => {
+    // Scan only the first kilo-byte to speed up the check on larger files
+    const fastScan = svgReg.test(toUTF8String(input, 0, 1000))
+    // The fast check could result in false negative but never false positive
+    if (fastScan) return true
+    return svgReg.test(toUTF8String(input))
+  },
   calculate(input) {
     const root = extractorRegExps.root.exec(toUTF8String(input));
     if (root) {
