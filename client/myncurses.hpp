#include <bits/stdc++.h>
#include <ncurses.h>
#include <curses.h>
void easy_window_print(WINDOW *window_pointer, int x, int y, int maxX, int maxY, char output[])
{
    
} //the x,y defines where the input should start, then maxX and maxY shows the limit of texting