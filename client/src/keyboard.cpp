#include "libs/ncurses_utils.cpp"
#include <thread>
namespace keyboard
{
    inline void keyboard_event_handler() // This function should be run on a single thread
    {
    }
} // namespace keyboard
